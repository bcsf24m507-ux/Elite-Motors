services:
  - type: web
    name: php-app
    runtime: docker
    repo: https://github.com/yourusername/your-repo
    branch: main
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PHP_VERSION
        value: 8.2
    numInstances: 1
